(*DIEGO SUAREZ RAMOS- diego.suarez.ramos@udc.es*)
let f= function a->a;;
(*Podemos tener una única función que es la función identidad*)
let h= function a,b-> a;;
(*Podemos 2 la funcion que va de a,b.>a y la funcion que va de b,a->b*)
let i= function a,b-> b;;
(*Podemos 2 la funcion que va de a,b.>b y la funcion que va de b,a->a*)
let j= function a-> [a];;
(*Podemos tener una única función que es la que te convierte un a en un a list*)

